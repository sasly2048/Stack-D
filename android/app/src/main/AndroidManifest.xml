<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Sessions keep a live timer in a foreground notification so remaining
         time stays visible while the screen is off. Declared here in Phase 0;
         the service itself lands with the room screen in Phase 1. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Breach detection reads tilt and motion. Neither needs a runtime
         permission, but both are required for the app to do its job. -->
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="true" />

    <application
        android:name=".StackdApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Stackd">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:theme="@style/Theme.Stackd">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Keeps the remaining time glanceable while a session runs and the
             app is off screen. "specialUse" because none of the standard types
             describe a countdown the user deliberately started; the reason
             below is what Play review reads. -->
        <service
            android:name=".feature.room.session.FocusSessionService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Displays the remaining time of a focus session the user started, so it stays visible while the screen is off." />
        </service>

    </application>

</manifest>
